import {StyleSheet} from 'react-native';
import {BUTTON_PAGINATION_SIZE} from '../../utils/constants';

export const styles = StyleSheet.create({
  image: {
    width: BUTTON_PAGINATION_SIZE,
    height: BUTTON_PAGINATION_SIZE,
  },
});
