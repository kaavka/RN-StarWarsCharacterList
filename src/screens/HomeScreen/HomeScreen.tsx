import {CharatersTable} from '../../components/CharactersTable/CharactersTable';
import {SafeAreaView} from 'react-native-safe-area-context';

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <CharatersTable />
    </SafeAreaView>
  );
};

export default HomeScreen;
